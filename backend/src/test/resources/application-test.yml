spring:
  main:
    banner-mode: "off"
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

logging:
  level:
    root: WARN

app:
  security:
    jwt:
      secret: test-jwt-secret-with-at-least-32-bytes
