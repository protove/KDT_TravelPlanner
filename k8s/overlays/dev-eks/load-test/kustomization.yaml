apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# This overlay is rendered only for the SCRUM-80 live breakpoint run. It
# leaves the canonical dev-eks workload and its base HPA at 2-4 untouched.
resources:
  - ../workload
patches:
  - path: backend-hpa-node-scale.patch.yaml
  - path: backend-google-api-mock.patch.yaml
